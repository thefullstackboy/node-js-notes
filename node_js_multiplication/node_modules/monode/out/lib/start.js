(function() {
  var monome;

  monome = require('./monome')();

  monome.on('connect', function(device) {
    return device.on('key', function(x, y, i) {
      return device.led(x, y, i);
    });
  });

}).call(this);
